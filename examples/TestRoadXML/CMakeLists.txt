

LINK_DIRECTORIES(${XERCESC_DIR}/lib)

ADD_EXECUTABLE(TestRoadXML 
TestRoadXML.cpp
TestRoadXML.h
)

IF(WIN32)
	TARGET_LINK_LIBRARIES(TestRoadXML RoadXML)
ELSE(WIN32)
	TARGET_LINK_LIBRARIES(TestRoadXML RoadXML stk xerces-c)
ENDIF(WIN32)

install (TARGETS TestRoadXML DESTINATION bin)
